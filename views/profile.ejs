<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Profile</title>
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body>

  <div class="w-full min-h-screen bg-zinc-900 text-white p-10"> 
  <div class=" w-full flex justify-end"> 
    <a class="bg-red-500 rounded-md px-3 py-2 text-sm mb-2 inline-block" href="/logout">Logout</a> 
  </div> 
  <h3 class="text-3xl mb-2">Hello, <%= user.name %>👋 </h3> 
  <h4 class="mb-5">You can create a post.</h4> 
  <form method="post" action="/post"> <textarea placeholder="What's on your mind ? " class="block w-1/3 p-3 outline-none bg-transparent border-2 border-zinc-800 rounded-md" name="content"></textarea> 
    <input class=" w-30 px-3 py-2 bg-blue-500 rounded-md mt-2 text-sm" type="submit" value="Create new post"> 
  </form> 
  <div class="posts mt-20"> 
    <h3 class="text-md text-zinc-400">Your Posts.</h3> 
    <div class="postcontainer mt-5"> <% user.posts.reverse().forEach(function(post){ %> 
      <div class="post w-1/3 p-4 m-4 rounded-md border-2 border-zinc-700 bg-zinc-800"> 
        <h4 class="text-blue-500 mb-2">@<%= user.username %></h4> 
        <p class="text-sm trackingi-tight"><%= post.content %></p> 
        <small class="mt-2 inline-block"><%= post.likes.length %> likes</small> 
        <div class="btns flex gap-4 mt-1"><form action="/like/<%= post._id %>" method="POST" class="inline">
           <button type="submit" class="text-blue-500">
             <%= post.likes.indexOf(user._id) === -1 ? "Like" : "Unlike" %>
            </button>
          </form>

          <a class="text-zinc-600" href="/edit/<%= post._id %>">Edit</a>
         <form action="/delete/<%= post._id %>" method="post" class="inline">
             <button class="text-blue-500 hover:text-red-400 text-sm" type="submit">Delete</button>
         </form>

        </div> 
      </div> <% }) %> 
        </div> 

       



        </div> 
      </div> 
    </body>
</html>
